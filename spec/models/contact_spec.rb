require 'spec_helper'

describe Contact do

  it "should find a matching pair if one exists" do
    it "contact#find_pair should find an existing matching pair" do
    end

    it "contact#find_pair should return nil if no match exists" do
    end
  end

  it "should create a Mailbox, first post, and associated BoxMemberships if there is a match" do
    it "contact#create_mailbox should create a new Mailbox" do
    end

    it "contact#create_mailbox should create the first Post for the new Mailbox" do
    end

    it "contact#create_mailbox should create a BoxMembership for each of the two users" do
    end
  end

end
