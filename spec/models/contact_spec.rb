require 'spec_helper'

describe Contact do

  describe "contact#find_pair - should find a matching pair if one exists" do
    it "should find an existing matching pair" do
    end

    it "should return nil if no match exists" do
    end
  end

  describe "contact#create_mailbox - should create a Mailbox, first post, and associated BoxMemberships if there is a match" do
    it "should create a new Mailbox" do
    end

    it "should create the first Post for the new Mailbox" do
    end

    it "should create a BoxMembership for each of the two users" do
    end
  end

end
